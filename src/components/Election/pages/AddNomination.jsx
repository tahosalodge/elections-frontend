import React, { Fragment as F } from 'react';

const AddNomination = ({ match }) => (
  <F>
    {JSON.stringify(match)}
    <h1>Nomination form goes here</h1>
  </F>
);

export default AddNomination;
